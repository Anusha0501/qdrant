# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# Data / model artifacts.
# Ignore generated chunks, but keep the labeled eval set in git.
# `data/` (trailing slash) would ignore the directory itself and make the
# negation below impossible — ignore the contents instead.
data/*
*.jsonl
!data/eval_queries.jsonl
.cache/
local_qdrant/

# Notebooks checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
